
Assignment –2
Introducing Relational Databases.

1) Which field of the Customers table is the primary key?
1)->CREATE TABLE CUSTOMERS (
    Cnum INT(4) PRIMARY KEY,
    Cname VARCHAR(10),
    City VARCHAR(10),
    Rating INT(4),
    Snum INT(4)
);
This ensures that no two records can have the same value for Cnum.
Here primary key is Cnum.

2) What is the 4th column of the Customers table?
2)->Rating ist the 4th column of the customers table

3) What is another word for row? For column?
3)Another word for Row = Record or tuple
			for	Column = Field or attribute

4) Why isn’t it possible to see the first five rows of a table?
=> In SQL, unless you use a query like SELECT * FROM table_name LIMIT 5; , the database doesn't automatically return only the first five rows.  
